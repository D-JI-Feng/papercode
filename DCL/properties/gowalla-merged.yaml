load_col:
  inter: [user_id, item_id]

user_inter_num_interval: "[10,inf)"
item_inter_num_interval: "[10,inf)"

train_batch_size: 4096
dataset_save_path: './saved/gowalla-merged-Dataset.pth'

train_neg_sample_args:
  sample_num: 10
